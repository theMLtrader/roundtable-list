{
  "name": "camelot-roundtable-list",
  "version": "1.0.0",
  "author": "0xmyrddin",
  "license": "GPLV3",
  "scripts": {
    "build": "mkdir -p build && node src/write.js > build/roundtable.json && cp _redirects build/"
  },
  "files": [
    "build/roundtable.json"
  ],
  "devDependencies": {}
}
